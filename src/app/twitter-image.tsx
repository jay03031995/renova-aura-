/**
 * X/Twitter share card — identical to the Open Graph card.
 * Re-exporting keeps a single source of truth for the design.
 */
export { default, alt, size, contentType } from "./opengraph-image";
